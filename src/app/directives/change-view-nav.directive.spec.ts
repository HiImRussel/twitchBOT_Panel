import { ChangeViewNavDirective } from './change-view-nav.directive';

describe('ChangeViewNavDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeViewNavDirective();
    expect(directive).toBeTruthy();
  });
});
